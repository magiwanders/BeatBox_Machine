class Clock {
  constructor(id, gridOfDots, isArmed, tempo, quantization) {
    this.id = id
    this.gridOfDots = gridOfDots
    this.isArmed = isArmed
    this.tempo = tempo
    this.quantization = quantization
  }

  getId() {
    return this.id
  }

  getDotArray(layer) {
    return 
  }
}
