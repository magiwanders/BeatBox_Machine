class Default {

    static DEBUG = false

    // Clock appeareance
    static innerCircleRadius = 93
    static handOverhang = 10
    static handWidth = 2
    static dotDiameter = 10
    static borderThickness = 2
    static handHeight = this.innerCircleRadius + this.handOverhang
    static clockMargin = this.innerCircleRadius * 2 / 10
    static circleDiameter = 2 * this.innerCircleRadius
}
